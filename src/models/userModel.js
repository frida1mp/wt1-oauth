/* eslint-disable jsdoc/require-param */
/**
 *
 */
export class User {
  // eslint-disable-next-line jsdoc/require-description
  /**
   *
   */
  constructor ({ id, username, name, avatar_url, email }) {
    this.id = id
    this.username = username
    this.name = name
    this.avatarUrl = avatar_url
    this.email = email
  }
}
